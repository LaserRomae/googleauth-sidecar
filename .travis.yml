language: python
python:
  - "3.6"

branches:
  only:
    - development

addons:
  sonarcloud:
    organization: "laserromae"

script:
  - nosetests --with-coverage --cover-branches --cover-xml src/test/unittest.py
  - sonar-scanner